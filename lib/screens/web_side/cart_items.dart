import 'package:flutter/material.dart';

class CartItemsScreen extends StatelessWidget {
  static const String id = 'cartItems';
  CartItemsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CART ITEMS SCREEN'),
    );
  }
}
